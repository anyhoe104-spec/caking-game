# 工房演出改修 検証記録

2026-09-07 / Codex / main 746c3f9からの未コミット差分

## 自動検証

- `npm test`: 20/20合格。既存16件＋パーツ購入・装備・移行・保存の4件。
- `npm run lint`: 警告・エラー0。
- `npm run build` / `npm run build:root`: 両方成功。`git diff --check`も成功。
- Chromium 149 headless + Playwright、Viteサーバーでブラウザ検証。390×844を基本とする。

## ブラウザで確認したシナリオ

1. オープニングをスキップ → 営業開始 → ショップに待ち客2人を表示。
2. 客をタップ → 該当レシピへ移動 → つくるボタンを同一処理内で2回発火してもcraftCount増分は1。
3. 製造演出をスキップ → 工程タイマーが残っていても完成状態から巻き戻らない → 工房へ戻る。
4. ハーブをPで購入 → 装備 → 有料王冠を試着 → 保存される装備はハーブのまま → リロードでも保持。有料購入ボタンは無効。
5. 営業残り1秒で製造 → 営業終了後も製造結果が可視 → 確認後に日報 → 翌日。
6. レベル10・所持金100,001Pのテスト状態で製造 → 完成確認 → エンディング → つづける → 再度製造が可能。
7. システムのreduced-motionを有効化 → 工程を省略して完成を表示。
8. 幅320/390/430/768px × 営業/レシピ/デコレーション/食材/スタッフの20組で横方向のはみ出しなし。
9. JavaScript pageerrorなし。

5と6は保存データをテスト用に変更して境界を再現したもので、最初から最後までの自然なバランス評価ではない。

## 視覚確認

ホームとおめかし画面のスクリーンショットを確認。店内背景・ミニキャラ・試着プレビュー・購入カードを描画。検証用Linuxには日本語・絵文字フォントがなく、日本語本文に検証用フォントを追加した。OS固有の明朝体・絵文字表示は実機確認が必要。フォントは本番依存やゲーム素材には追加していない。

## 制限

- Android/iPhone実機・Safari・署名ビルド・本番配信URLの新差分は未検証。
- 60fps、消費電力、音声品質、低速通信、長時間のゲームバランスは未測定。
- ネイティブ課金は未実装。有料パーツは試着のみ。
- レシピ別の本格的な製造工程と、そのケーキ自体への装飾合成は次工程。現状は汎用ケーキのプレビューを併用。

## 再開後の追加検証

全8レシピをレベル10のテスト状態で製造し、24工程それぞれの小道具、装飾時の完成形を確認。前回の境界操作と4幅×5画面も再検証して合格。lint警告0、20/20テスト、通常/rootビルド成功。

再実行用: `scripts/verify-atelier.cjs`。Playwrightを利用できる開発環境で `node scripts/verify-atelier.cjs` を実行する。標準のChromiumを事前に導入するか、`PLAYWRIGHT_EXECUTABLE` で実行ファイルを指定する。`PLAYWRIGHT_MODULE` は標準外のPlaywright配置用。スクリーンショットは既定でOSの一時フォルダへ出力する。実機品質の合否を代替するものではない。

上の「レシピ別工程は次工程」は初回の記録。現在はレシピ固有演出とSVGの形状別装飾が実装済み。既存画像と装飾時SVGの美術的な統一は未完。

## 2026-09-07 22:56 +0900 再開時の再検証

- 営業/製造中の非表示・一時停止・再読み込みと明示再開を検証。
- 全8レシピ24工程、日報、エンディング継続、4幅×5画面が合格。pageerrorなし。
- 実時間による残り1秒と工程確認は環境負荷で失敗したため、Playwright時計を制御して同じ境界を確実に再現する方式に変更。スキップ前の自然完了も受け入れる。
- 通常配布のオフライン再起動、保存維持、42画像を検証。
- lint/20単体テスト/通常build/native:sync成功。AndroidテストIDを修正したが、端末上のinstrumentation testとネイティブコンパイルは未実行。

## おめかし取り外し・一覧プレビューの追加

- lint成功、単体21件、通常build/native:sync成功。
- ブラウザで6種のレシピ形状プレビュー、帯取り外し、定番復帰、試着解除、通貨/所持保持、リロード後再装備を確認。
- 全8レシピ24工程の通過を確認。新しいおめかしの操作は独立したブラウザ実行でも合格。
- 実機17項目はdocs/device-test-record.mdのとおり未実施。手順はdocs/release-runbook.md。

検証補足: 今回の通しスクリプト初回は、追加ケースのレシピ名を「とろけるプリン」と誤記して選択待ちで停止した。実装データの「プリン」へ修正し、修正した追加ケースを独立実行して全件合格。全通しの再実行完了は今回主張しない。

## 2026-09-08 保存・製造計算・操作仕上げ

- 単体33件：破損時の控え/旧版復旧、バックアップの検証・報酬重複防止・有料権利除外、容量不足、製造の売上/スタッフ/ミッション、全レベルでの来客目標到達可能性。
- `scripts/verify-atelier.cjs` を全通し実行し合格。8レシピ24工程、二重製造防止、中断/リロード、日報/翌日、エンディング/継続、試着/購入/帯の取り外し/再装備、4幅×5画面。
- `scripts/verify-save.cjs`：設定中の時間停止、全文書き出し、不正データ拒否、復元確認/キャンセル/実行、復元後リロードと明示再開、音量操作のフォーカス維持、Tab循環/Escape復帰、3幅、保存容量不足時の復元中止と再試行、控えからの復旧通知。
- `scripts/verify-playthrough.cjs`：セーブ注入なしの新規開始から、自動回復・注文優先製造・日報を繰り返してエンディングと継続まで合格。今回のランダム結果は143製造、8日目、Lv11、100,050P、ゲーム内1440秒。時間はPlaywrightで早送りし動きを軽減。実時間の遊び心地・自然な操作速度・実機フレームレートの証明ではない。
- 検証中の修正：最初のブラウザ起動は実行ファイルの欠損で失敗し復元。設定からのフォーカス復帰が失敗したため、開いた要素を明示保持して修正。最初の通しプレイは時計の差し替えが起動後だったため回復タイマーが実時間に残り失敗。起動前に時計を設置して再実行した。
- Linuxの検証画像は日本語フォントが不足し、ストア掲載には利用しない。署名/実機/実課金は未実施。
- オフライン検証の初回は、移行後に不要な顧客属性と報酬の既定値が正規化されるため、JSON文字列の完全一致で失敗。既知の進行フィールド全体を正規化して比較するよう検証を修正した（タイマー・所持金・素材・注文状況も比較対象）。
- lint・通常build・ルートbuild・Capacitor syncは成功。通常 `/caking-game/` とルート `/` の両方でオフライン再起動、進行保持、4画面、42画像デコードを再確認して合格。
